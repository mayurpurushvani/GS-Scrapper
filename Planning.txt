function app(run = (link scrap/product scrap))

seprate file for link
    function run(link)
seprate file for product scrap
    function run(product)
        loop : fetch products link from link table (limited - threads):
            scrap()
                Add data into product table
                update the flag into link table